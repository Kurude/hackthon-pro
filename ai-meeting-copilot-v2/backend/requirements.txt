fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
groq==0.11.0
chromadb==0.5.5
gTTS==2.5.3
pypdf==4.3.1
python-docx==1.1.2
pydantic==2.9.2
python-dotenv==1.0.1

# NOTE: sentence-transformers / torch were intentionally removed.
# ChromaDB ships its own lightweight built-in embedding model (ONNX-based
# all-MiniLM-L6-v2) so RAG works out of the box with a much smaller,
# much more reliable install - no PyTorch download needed at all.
